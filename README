## 🗳️ Голосование за лучших сотрудников

Веб-приложение на Flask для проведения голосования в различных номинациях (например, "Лидер года", "Наставник года"). Пользователь может отдать свой голос за кандидата в каждой номинации. Поддерживается возможность изменить свой выбор. Список кандидатов и номинаций задаётся через базу данных и отдельные файлы конфигурации.

---

### 🔧 Стек технологий

* Python 3.x
* Flask
* SQLAlchemy (ORM)
* Flask-Migrate
* HTMX (асинхронные запросы без перезагрузки)
* HTML + Jinja2
* SQLite (по умолчанию)

---

### 📁 Структура проекта

```
your_project/
├── app.py                # основной файл Flask-приложения
├── config.py             # конфигурация приложения
├── models.py             # модели базы данных (Candidate, Nomination)
├── nominations.py        # список всех номинаций
├── migrations/           # миграции для базы данных (авто)
├── static/
│   └── images/logo.png   # логотип и прочие статики
├── templates/
│   ├── konkurs.html      # основной шаблон
│   └── _nomination_form.html  # переиспользуемый шаблон формы
├── .gitignore
└── requirements.txt
```

---

### ⚙️ Установка и запуск

1. **Клонируй репозиторий**:

```bash
git clone https://github.com/yourname/voting-app.git
cd voting-app
```

2. **Создай и активируй виртуальное окружение**:

```bash
python -m venv venv
source venv/bin/activate   # или venv\Scripts\activate на Windows
```

3. **Установи зависимости**:

```bash
pip install -r requirements.txt
```

4. **Создай базу данных и миграции**:

```bash
flask db init
flask db migrate -m "initial migration"
flask db upgrade
```

5. **Запусти приложение**:

```bash
flask run
```

По умолчанию откроется на `http://127.0.0.1:5000`

---

### 🗃️ Работа с базой

* Кандидаты (`Candidate`) добавляются через скрипт или админку.
* Номинации хранятся в файле `nominations.py`.

---

### 🧠 Особенности

* Один пользователь может голосовать за каждую номинацию только один раз (по cookie), но может изменить свой голос.
* Используется `uuid4` как уникальный идентификатор в cookie.
* Простая, адаптивная HTML-форма с HTMX для мгновенного отклика без перезагрузки страницы.

---

### ✅ Пример записи в базе

| Номинация  | Сотрудник         | Cookie ID                   |
| ---------- | ----------------- | --------------------------- |
| Лидер года | Азамат Азаматович | 16a1b54c-832a-4e6a-8d7a-... |

---

### 📦 requirements.txt (пример)

```txt
Flask
Flask-SQLAlchemy
Flask-Migrate
```

---

### 📌 TODO

* [ ] Добавить админ-панель (Flask-Admin)
* [ ] Защита от повторных голосов с одного IP (опционально)
* [ ] Поддержка загрузки аватаров
* [ ] Экспорт голосов в CSV

---

